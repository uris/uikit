import { Meta, Source, Unstyled } from '@storybook/addon-docs/blocks';
import {FlexDiv} from "../uikit/FlexDiv";
import {Logos} from "../uikit/Logos";
import {Spacer} from "../uikit/Spacer";

<Meta title="Welcome to Slice" />

<FlexDiv width={"fill"} height={"fill"} justify={"center"} alignItems={"center"}>
    <FlexDiv width={"fill"} height={"auto"}>
        <Logos image={"slice logo"} width={"100%"} />
        <Spacer size={36} />
        <Unstyled>
            Welcome to **Slice** - a free, flexible, fast rendering ui-kit for creating modern react apps (v18+). Add it to your project:
        </Unstyled>
        <Source
            language='bash'
            code={`
                $ npm install @apple-pie/slice
            `}
        />
        <Unstyled>
            Folks at [motion.dev](https://motion.dev) created an amazing animation library - Slice uses their work for fancy transitions! Make sure that's also in your project.
        </Unstyled>
        <Source
            language='bash'
            code={`
                $ npm install motion/react
            `}
        />
        <Unstyled>
            Ready for apple pie? Let's get some ...
        </Unstyled>
    </FlexDiv>
</FlexDiv>