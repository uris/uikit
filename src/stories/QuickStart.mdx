import { Meta, Source, Unstyled } from '@storybook/addon-docs/blocks';
import {FlexDiv} from "../uikit/FlexDiv";
import {Spacer} from "../uikit/Spacer";

<Meta title="Quick Start" />

<FlexDiv width={"fill"} height={"fill"} alignItems={"center"}>
    <FlexDiv width={"fill"} maxWidth={760} height={"auto"}>
        <Unstyled>
            # Quick Start
            Slice ships with a light and dark theme. To get set up, install Slice, wrap your app with the theme provider and use the included components.
            The built in `useTheme` hook makes styling and managing themes easy.
            
            #### 1. Install Slice and Motion
            Make sure Slice and [Motion](https://motion.dev) are installed in your project ...
        </Unstyled>
        <Source
            language='bash'
            code={`
                $ npm install @apple-pie/slice motion/react
            `}
        />
        <Unstyled>
            #### 2. Wrap your app with the theme provider
            Wrap your app (or where you plan to use Slice) with Slice's `ThemeProvider`. Set the system flag to true to have Slice automatically adopt the system color theme (dark / light mode)
        </Unstyled>
        <Source
            language='jsx'
            code={`
import { ThemeProvider } from '@apple-pie/slice';

// ...
      
<ThemeProvider system={true}>
    <App />
</ThemeProvider>

// ...      
`}
        />
        <Unstyled>
            #### 3. Use Slice components.
            Now you can and use Slice components anywhere inside `<App />`.
        </Unstyled>
        <Source
            language='jsx'
            code={`
import { Avatar } from '@apple-pie/slice';

// ...

<Avatar size={32} first={"John"} last={"Appleseed"} />
      
// ...
            
            `}
        />
        <Unstyled>
            #### 4. Use the Slice theme in your own components.
            Use the built in hook to grab the current theme and access tokens or values for use in your components. InteliSense will pick up all the available tokens as you type - no need to remeber them all.
        </Unstyled>
        <Source
            language='jsx'
            code={`
import { useTheme } from '@apple-pie/slice';

// ...
            
const theme = useTheme();
      
// Use theme.current.colors to get actual values from the active theme
// this will return the value "#0000ff"
        
<MyLabel textColor={theme.current.colors["core-link-primary"]}>
    My Custom Label
</MyLabel>
            
// Use theme.colors to get the css token itself.
// this will return the value "var(--core-link-primary)" 

<span style={{color:theme.colors["core-link-primary"]}>
    My Custom Label
</span>
            
// ...                  
`}
        />
        <Unstyled>
            There are a few options to styling your own components. Learn more about the useTheme hook here.
            #### 5. Toggle light / dark modes
            The useTheme hook allows you to easily toggle or set the current theme.
        </Unstyled>
        <Source
            language='jsx'
            code={`
import { useTheme, lightTheme, UIButton } from '@apple-pie/slice';

// ...
            
const theme = useTheme();
            
// ...

// toggle light / dark theme            
<UIButton label={"Toggle Theme"} onClick={() => theme.toggle()} />

// set theme by name / object - pass "system" to use your PC theme              
<UIButton label={"Light Mode"} onClick={() => theme.set(lightTheme)} />
`}
        />
        <Unstyled>
            <Spacer size={24} />
            <p className={"note"}>Note: Slice uses css modules for styling and should play nice with other tools your project uses to manage styles. It also makes Slice compatible with mixed or server side architectures like [Next.js](https://nextjs.org) and [Remix](https://remix.run).</p>
        </Unstyled>
    </FlexDiv>
   
</FlexDiv>