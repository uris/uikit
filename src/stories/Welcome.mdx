import { Meta, Source, Unstyled } from '@storybook/addon-docs/blocks';
import {FlexDiv} from "../uikit/FlexDiv";
import {Logos} from "../uikit/Logos";
import {Spacer} from "../uikit/Spacer";

<Meta title="Welcome" />

<FlexDiv width={"fill"} height={"fill"} justify={"center"} alignItems={"center"}>
    <FlexDiv width={"fill"} maxWidth={760} height={"auto"}>
        <Logos image={"slice logo"} width={"100%"} />
        <Spacer size={36} />
        <Unstyled>
            ✌️Welcome to **Slice** - a free, flexible, and fast ui-kit for creating modern react apps (v18+). Start by adding it to your project:
        </Unstyled>
        <Source
            language='bash'
            code={`
                $ npm install @apple-pie/slice
            `}
        />
        <Unstyled>
            ##### Dependencies
            Folks at [motion.dev](https://motion.dev) created an amazing animation library - Slice uses their work to do some fancy stuff. Make sure that's also in your project.
        </Unstyled>
        <Source
            language='bash'
            code={`
                $ npm install motion/react
            `}
        />
        <Unstyled>
            That should do it. Let's grab a slice ...
        </Unstyled>
    </FlexDiv>
</FlexDiv>