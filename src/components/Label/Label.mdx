import {Meta, Unstyled} from '@storybook/addon-docs/blocks';
import {FlexDiv} from '../FlexDiv';
import * as LabelStories from './Label.stories';
import {RenderSource} from '../../../.storybook/RenderSource';

<Meta of={LabelStories} />

<FlexDiv width={"fill"} height={"fill"} alignItems={"center"}>
    <FlexDiv width={"fill"} maxWidth={760} height={"auto"}>
        <Unstyled>
        # Label
        Renders a styled text label/pill for statuses, tags, and inline annotations.

        ## Base element
        `HTMLDivElement`

        ## Interface
        `LabelProps`

        ## Props
        <table style={{width: '100%'}}>
          <colgroup>
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '97%'}} />
          </colgroup>
          <thead>
            <tr>
              <th className={"left"}>Prop</th>
              <th className={"left"}>Type</th>
              <th className={"left"}>Default</th>
              <th>Description</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>button</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `button`.</td></tr>
            <tr><td><code>children</code></td><td><code>React.ReactNode</code></td><td><code>-</code></td><td>Label content rendered inside the component.</td></tr>
            <tr><td><code>noFill</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `noFill`.</td></tr>
            <tr><td><code>round</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `round`.</td></tr>
            <tr><td><code>size</code></td><td><code>{`'s' | 'm' | 'l'`}</code></td><td><code>'m'</code></td><td>Controls label typography size.</td></tr>
            <tr><td><code>padding</code></td><td><code>number | string</code></td><td><code>-</code></td><td>Custom padding. Number values are converted to pixel units.</td></tr>
            <tr><td><code>border</code></td><td><code>number</code></td><td><code>1</code></td><td>Border width in pixels. Set to <code>0</code> to remove the border.</td></tr>
            <tr><td><code>state</code></td><td><code>{`'red' | 'yellow' | 'green' | 'blue' | 'grey' | 'lightgrey' | 'white'`}</code></td><td><code>-</code></td><td>Controls `state`.</td></tr>
            <tr><td><code>...divAttributes</code></td><td><code>{`HTMLAttributes<HTMLDivElement>`}</code></td><td><code>-</code></td><td>Native `div` props pass through (for example `id`, `data-testid`, `className`, `style`, `aria-*`).</td></tr>
          </tbody>
        </table>

        ## Events
        <table style={{width: '100%'}}>
          <colgroup>
            <col style={{width: '1%'}} />
            <col style={{width: '99%'}} />
          </colgroup>
          <thead>
            <tr>
              <th className={"left"}>Event</th>
              <th className={"left"}>Type</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>onClick</code></td><td><code>{`(e: React.MouseEvent<any>) => void`}</code></td></tr>
          </tbody>
        </table>

        ## Code Examples
        </Unstyled>

        <RenderSource language={"tsx"}>
{`
import { Label } from '@apple-pie/slice';

export function LabelExample() {
  return (
    <Label
      size="m"
      state="green"
      border={1}
      padding="4px 10px"
      round={true}
      id="label-example"
      data-testid="label-example"
      className="custom-wrapper"
      style={{ marginTop: 8, maxWidth: 480 }}
    >
      Connected
    </Label>
  );
}
`}
        </RenderSource>
    </FlexDiv>
</FlexDiv>
