import {Meta, Unstyled} from '@storybook/addon-docs/blocks';
import {FlexDiv} from '../FlexDiv';
import * as DivInputStories from './DivInput.stories';
import {RenderSource} from '../../../documentation/_components/RenderSource/RenderSource';

<Meta of={DivInputStories} />

<FlexDiv width={"fill"} height={"fill"} alignItems={"center"}>
    <FlexDiv width={"fill"} maxWidth={760} height={"auto"}>
        <Unstyled>
        # DivInput
        Renders a content-editable input surface for rich text-like inline editing workflows.

        ## Base element
        `HTMLDivElement`

        ## Interface
        `DivInputProps`

        ## Props
        <table style={{width: '100%'}}>
          <colgroup>
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '97%'}} />
          </colgroup>
          <thead>
            <tr>
              <th className={"left"}>Prop</th>
              <th className={"left"}>Type</th>
              <th className={"left"}>Default</th>
              <th>Description</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>bgColor</code></td><td><code>string</code></td><td><code>-</code></td><td>Color token/value for `bgColor`.</td></tr>
            <tr><td><code>clamp</code></td><td><code>number</code></td><td><code>-</code></td><td>Controls `clamp`.</td></tr>
            <tr><td><code>focus</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `focus`.</td></tr>
            <tr><td><code>isEditable</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `isEditable`.</td></tr>
            <tr><td><code>name</code></td><td><code>string</code></td><td><code>-</code></td><td>Text/value for `name`.</td></tr>
            <tr><td><code>padding</code></td><td><code>string</code></td><td><code>-</code></td><td>Sizing/layout setting for `padding`.</td></tr>
            <tr><td><code>placeholder</code></td><td><code>string</code></td><td><code>-</code></td><td>Text/value for `placeholder`.</td></tr>
            <tr><td><code>radius</code></td><td><code>number</code></td><td><code>-</code></td><td>Sizing/layout setting for `radius`.</td></tr>
            <tr><td><code>textAlign</code></td><td><code>{`'left' | 'center' | 'right'`}</code></td><td><code>-</code></td><td>Controls `textAlign`.</td></tr>
            <tr><td><code>value</code></td><td><code>string</code></td><td><code>-</code></td><td>Text/value for `value`.</td></tr>
            <tr><td><code>width</code></td><td><code>{`number | string`}</code></td><td><code>-</code></td><td>Sizing/layout setting for `width`.</td></tr>
            <tr><td><code>wrap</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `wrap`.</td></tr>
            <tr><td><code>...divAttributes</code></td><td><code>{`HTMLAttributes<HTMLDivElement>`}</code></td><td><code>-</code></td><td>Native `div` props pass through (for example `id`, `data-testid`, `className`, `style`, `aria-*`).</td></tr>
          </tbody>
        </table>

        ## Events
        <table style={{width: '100%'}}>
          <colgroup>
            <col style={{width: '1%'}} />
            <col style={{width: '99%'}} />
          </colgroup>
          <thead>
            <tr>
              <th className={"left"}>Event</th>
              <th className={"left"}>Type</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>onClick</code></td><td><code>{`() => void`}</code></td></tr>
            <tr><td><code>onDblClick</code></td><td><code>{`() => void`}</code></td></tr>
            <tr><td><code>onChange</code></td><td><code>{`(value: string | null) => void`}</code></td></tr>
            <tr><td><code>onSubmit</code></td><td><code>{`(value: string) => void`}</code></td></tr>
            <tr><td><code>onFocus</code></td><td><code>{`(event: React.FocusEvent<HTMLDivElement>) => void`}</code></td></tr>
            <tr><td><code>onBlur</code></td><td><code>{`(value: string) => void`}</code></td></tr>
          </tbody>
        </table>

        ## Code Examples
        </Unstyled>

        <RenderSource language={"tsx"}>
```
import { DivInput } from '@apple-pie/slice';

export function DivInputExample() {
    return (
        <DivInput
            name="Project Name"
            value="Policy Review"
            placeholder="Enter title"
            onChange={(value) => console.log(value)}
            id="divinput-example"
            data-testid="divinput-example"
            className="custom-wrapper"
            style={{ marginTop: 8, maxWidth: 480 }}
        />
    );
}
```
        </RenderSource>
    </FlexDiv>
</FlexDiv>
