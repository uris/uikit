import {Meta, Unstyled} from '@storybook/addon-docs/blocks';
import {FlexDiv} from '../FlexDiv';
import * as FileIconStories from './FileIcon.stories';
import {RenderSource} from '../../../documentation/_components/RenderSource/RenderSource';

<Meta of={FileIconStories} />

<FlexDiv width={"fill"} height={"fill"} alignItems={"center"}>
    <FlexDiv width={"fill"} maxWidth={760} height={"auto"}>
        <Unstyled>
        # FileIcon
        Renders a file-type icon for document formats such as PDF, text, spreadsheets, media, and code.

        ## Base element
        `SVGSVGElement`

        ## Interface
        `FileIconProps`

        ## Props
        <table style={{width: '100%'}}>
          <colgroup>
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '97%'}} />
          </colgroup>
          <thead>
            <tr>
              <th className={"left"}>Prop</th>
              <th className={"left"}>Type</th>
              <th className={"left"}>Default</th>
              <th>Description</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>name</code></td><td><code>string</code></td><td><code>'pdf'</code></td><td>Icon name key from the file icon registry (for example values from <code>FileIconNames</code>).</td></tr>
            <tr><td><code>size</code></td><td><code>number</code></td><td><code>24</code></td><td>Sizing/layout setting for `size`.</td></tr>
            <tr><td><code>pointer</code></td><td><code>boolean</code></td><td><code>true</code></td><td>Boolean flag controlling pointer cursor and keyboard focusability.</td></tr>
            <tr><td><code>disabled</code></td><td><code>boolean</code></td><td><code>false</code></td><td>Boolean flag controlling disabled visual and interaction state.</td></tr>
            <tr><td><code>...svgAttributes</code></td><td><code>{`SVGAttributes<SVGSVGElement>`}</code></td><td><code>-</code></td><td>Native `svg` props pass through (for example `id`, `data-testid`, `className`, `style`, `aria-*`).</td></tr>
          </tbody>
        </table>

        ## Events
        <table style={{width: '100%'}}>
          <colgroup>
            <col style={{width: '1%'}} />
            <col style={{width: '99%'}} />
          </colgroup>
          <thead>
            <tr>
              <th className={"left"}>Event</th>
              <th className={"left"}>Type</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>onClick</code></td><td><code>{`(e: React.MouseEvent<SVGElement, MouseEvent> | React.KeyboardEvent<SVGSVGElement>) => void`}</code></td></tr>
          </tbody>
        </table>

        ## Code Examples
        </Unstyled>

        <RenderSource language={"tsx"}>
```
import { FileIcon } from '@apple-pie/slice';

export function FileIconExample() {
    return (
        <FileIcon
            name="pdf"
            size={24}
            pointer
            onClick={() => console.log('clicked')}
            id="fileicon-example"
            data-testid="fileicon-example"
            className="custom-wrapper"
            style={{ display: 'block', marginTop: 8 }}
        />
    );
}
```
        </RenderSource>
    </FlexDiv>
</FlexDiv>
