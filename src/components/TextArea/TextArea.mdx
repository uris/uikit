import {Meta, Unstyled} from '@storybook/addon-docs/blocks';
import {FlexDiv} from '../FlexDiv';
import * as TextAreaStories from './TextArea.stories';
import {RenderSource} from '../../../documentation/_components/RenderSource/RenderSource';

<Meta of={TextAreaStories} />

<FlexDiv width={"fill"} height={"fill"} alignItems={"center"}>
    <FlexDiv width={"fill"} maxWidth={760} height={"auto"}>
        <Unstyled>
        # TextArea
        Renders a multi-line text input with label, validation, and optional action controls.

        ## Base element
        `HTMLDivElement`

        ## Interface
        `TextAreaProps`

        ## Props
        <table style={{width: '100%'}}>
          <colgroup>
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '97%'}} />
          </colgroup>
          <thead>
            <tr>
              <th className={"left"}>Prop</th>
              <th className={"left"}>Type</th>
              <th className={"left"}>Default</th>
              <th>Description</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>bgColor</code></td><td><code>string</code></td><td><code>-</code></td><td>Color token/value for `bgColor`.</td></tr>
            <tr><td><code>border</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Sizing/layout setting for `border`.</td></tr>
            <tr><td><code>borderRadius</code></td><td><code>number</code></td><td><code>-</code></td><td>Sizing/layout setting for `borderRadius`.</td></tr>
            <tr><td><code>disabled</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `disabled`.</td></tr>
            <tr><td><code>focused</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `focused`.</td></tr>
            <tr><td><code>hasSend</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `hasSend`.</td></tr>
            <tr><td><code>height</code></td><td><code>{`number | string`}</code></td><td><code>-</code></td><td>Sizing/layout setting for `height`.</td></tr>
            <tr><td><code>minWidth</code></td><td><code>{`number | string`}</code></td><td><code>-</code></td><td>Sizing/layout setting for `minWidth`.</td></tr>
            <tr><td><code>name</code></td><td><code>string</code></td><td><code>-</code></td><td>Text/value for `name`.</td></tr>
            <tr><td><code>padding</code></td><td><code>{`number | string`}</code></td><td><code>-</code></td><td>Sizing/layout setting for `padding`.</td></tr>
            <tr><td><code>placeholder</code></td><td><code>string</code></td><td><code>-</code></td><td>Text/value for `placeholder`.</td></tr>
            <tr><td><code>resizable</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `resizable`.</td></tr>
            <tr><td><code>returnSubmits</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `returnSubmits`.</td></tr>
            <tr><td><code>rows</code></td><td><code>number</code></td><td><code>-</code></td><td>Controls `rows`.</td></tr>
            <tr><td><code>sendOffset</code></td><td><code>{`{ bottom: number; right: number }`}</code></td><td><code>-</code></td><td>Controls `sendOffset`.</td></tr>
            <tr><td><code>sendSize</code></td><td><code>number</code></td><td><code>-</code></td><td>Sizing/layout setting for `sendSize`.</td></tr>
            <tr><td><code>submitClears</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `submitClears`.</td></tr>
            <tr><td><code>textSize</code></td><td><code>{`'s' | 'm' | 'l'`}</code></td><td><code>-</code></td><td>Sizing/layout setting for `textSize`.</td></tr>
            <tr><td><code>validate</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `validate`.</td></tr>
            <tr><td><code>value</code></td><td><code>string</code></td><td><code>-</code></td><td>Text/value for `value`.</td></tr>
            <tr><td><code>width</code></td><td><code>{`number | string`}</code></td><td><code>-</code></td><td>Sizing/layout setting for `width`.</td></tr>
            <tr><td><code>...divAttributes</code></td><td><code>{`HTMLAttributes<HTMLDivElement>`}</code></td><td><code>-</code></td><td>Native `div` props pass through (for example `id`, `data-testid`, `className`, `style`, `aria-*`).</td></tr>
          </tbody>
        </table>

        ## Events
        <table style={{width: '100%'}}>
          <colgroup>
            <col style={{width: '1%'}} />
            <col style={{width: '99%'}} />
          </colgroup>
          <thead>
            <tr>
              <th className={"left"}>Event</th>
              <th className={"left"}>Type</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>onChange</code></td><td><code>{`(value: string) => void`}</code></td></tr>
            <tr><td><code>onSubmit</code></td><td><code>{`(value: string) => void`}</code></td></tr>
            <tr><td><code>onFocus</code></td><td><code>{`() => void`}</code></td></tr>
            <tr><td><code>onBlur</code></td><td><code>{`() => void`}</code></td></tr>
            <tr><td><code>onValidate</code></td><td><code>{`(state: boolean) => void`}</code></td></tr>
            <tr><td><code>onKeyDown</code></td><td><code>{`(e: React.KeyboardEvent<HTMLTextAreaElement>) => void`}</code></td></tr>
          </tbody>
        </table>

        ## Code Examples
        </Unstyled>

        <RenderSource language={"tsx"}>
```
import { TextArea } from '@apple-pie/slice';

export function TextAreaExample() {
    return (
        <TextArea
            name="comment"
            value="Initial notes"
            placeholder="Write your note"
            onChange={(value) => console.log(value)}
            id="textarea-example"
            data-testid="textarea-example"
            className="custom-wrapper"
            style={{ marginTop: 8, maxWidth: 480 }}
        />
    );
}
```
        </RenderSource>
    </FlexDiv>
</FlexDiv>
