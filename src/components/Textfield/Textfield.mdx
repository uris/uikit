import {Meta, Unstyled} from '@storybook/addon-docs/blocks';
import {FlexDiv} from '../FlexDiv';
import * as TextfieldStories from './Textfield.stories';
import {RenderSource} from '../../../.storybook/RenderSource';

<Meta of={TextfieldStories} />

<FlexDiv width={"fill"} height={"fill"} alignItems={"center"}>
    <FlexDiv width={"fill"} maxWidth={760} height={"auto"}>
        <Unstyled>
        # Textfield
        Renders a labeled single-line text input with validation, adornments, and state styling.

        ## Base element
        `HTMLDivElement`

        ## Interface
        `TextFieldProps`

        ## Props
        <table style={{width: '100%'}}>
          <colgroup>
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '97%'}} />
          </colgroup>
          <thead>
            <tr>
              <th className={"left"}>Prop</th>
              <th className={"left"}>Type</th>
              <th className={"left"}>Default</th>
              <th>Description</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>actionButton</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `actionButton`.</td></tr>
            <tr><td><code>backgroundColor</code></td><td><code>{`{ focused: string; blurred: string }`}</code></td><td><code>-</code></td><td>Color token/value for `backgroundColor`.</td></tr>
            <tr><td><code>borderColor</code></td><td><code>{`{ focused: string; blurred: string; error: string }`}</code></td><td><code>-</code></td><td>Color token/value for `borderColor`.</td></tr>
            <tr><td><code>borderRadius</code></td><td><code>{`number | string`}</code></td><td><code>-</code></td><td>Sizing/layout setting for `borderRadius`.</td></tr>
            <tr><td><code>borderType</code></td><td><code>{`'box' | 'underline' | 'none'`}</code></td><td><code>-</code></td><td>Sizing/layout setting for `borderType`.</td></tr>
            <tr><td><code>clearBlurs</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `clearBlurs`.</td></tr>
            <tr><td><code>clearButton</code></td><td><code>{`{ size?: number } | null`}</code></td><td><code>-</code></td><td>Controls `clearButton`.</td></tr>
            <tr><td><code>color</code></td><td><code>{`{ focused: string; blurred: string; error: string; placeholder: string; disabled: string; label: string; }`}</code></td><td><code>-</code></td><td>Color token/value for `color`.</td></tr>
            <tr><td><code>disabled</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `disabled`.</td></tr>
            <tr><td><code>editable</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `editable`.</td></tr>
            <tr><td><code>focused</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `focused`.</td></tr>
            <tr><td><code>iconLeft</code></td><td><code>{`{ name?: string; size?: number; color?: string }`}</code></td><td><code>-</code></td><td>Icon configuration for `iconLeft`.</td></tr>
            <tr><td><code>inline</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `inline`.</td></tr>
            <tr><td><code>inputType</code></td><td><code>{`'text' | 'password'`}</code></td><td><code>-</code></td><td>Controls `inputType`.</td></tr>
            <tr><td><code>isValid</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `isValid`.</td></tr>
            <tr><td><code>label</code></td><td><code>string</code></td><td><code>-</code></td><td>Text/value for `label`.</td></tr>
            <tr><td><code>labelSize</code></td><td><code>{`'s' | 'm' | 'l'`}</code></td><td><code>-</code></td><td>Sizing/layout setting for `labelSize`.</td></tr>
            <tr><td><code>maxLength</code></td><td><code>number</code></td><td><code>-</code></td><td>Controls `maxLength`.</td></tr>
            <tr><td><code>name</code></td><td><code>string</code></td><td><code>-</code></td><td>Text/value for `name`.</td></tr>
            <tr><td><code>noShow</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `noShow`.</td></tr>
            <tr><td><code>padding</code></td><td><code>string</code></td><td><code>-</code></td><td>Sizing/layout setting for `padding`.</td></tr>
            <tr><td><code>placeholder</code></td><td><code>string</code></td><td><code>-</code></td><td>Text/value for `placeholder`.</td></tr>
            <tr><td><code>size</code></td><td><code>{`{ width?: number | string; height?: number | string }`}</code></td><td><code>-</code></td><td>Sizing/layout setting for `size`.</td></tr>
            <tr><td><code>textAlign</code></td><td><code>{`'left' | 'center'`}</code></td><td><code>-</code></td><td>Controls `textAlign`.</td></tr>
            <tr><td><code>textSize</code></td><td><code>{`'s' | 'm' | 'l'`}</code></td><td><code>-</code></td><td>Sizing/layout setting for `textSize`.</td></tr>
            <tr><td><code>validate</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `validate`.</td></tr>
            <tr><td><code>value</code></td><td><code>string</code></td><td><code>-</code></td><td>Text/value for `value`.</td></tr>
            <tr><td><code>...divAttributes</code></td><td><code>{`HTMLAttributes<HTMLDivElement>`}</code></td><td><code>-</code></td><td>Native `div` props pass through (for example `id`, `data-testid`, `className`, `style`, `aria-*`).</td></tr>
          </tbody>
        </table>

        ## Events
        <table style={{width: '100%'}}>
          <colgroup>
            <col style={{width: '1%'}} />
            <col style={{width: '99%'}} />
          </colgroup>
          <thead>
            <tr>
              <th className={"left"}>Event</th>
              <th className={"left"}>Type</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>onChange</code></td><td><code>{`(value: string) => void`}</code></td></tr>
            <tr><td><code>onBlur</code></td><td><code>{`(value: string) => void`}</code></td></tr>
            <tr><td><code>onFocus</code></td><td><code>{`(value: string) => void`}</code></td></tr>
            <tr><td><code>onKeydown</code></td><td><code>{`(key: string, event: React.KeyboardEvent) => void`}</code></td></tr>
            <tr><td><code>onSubmit</code></td><td><code>{`(value: string) => void`}</code></td></tr>
            <tr><td><code>onPaste</code></td><td><code>{`(value: React.ClipboardEvent<HTMLInputElement>) => void`}</code></td></tr>
            <tr><td><code>onClear</code></td><td><code>{`() => void`}</code></td></tr>
            <tr><td><code>onAction</code></td><td><code>{`() => void`}</code></td></tr>
            <tr><td><code>onValidate</code></td><td><code>{`(state: boolean) => void`}</code></td></tr>
          </tbody>
        </table>

        ## Code Examples
        </Unstyled>

        <RenderSource language={"tsx"}>
{`
import { TextField } from '@apple-pie/slice';

export function TextFieldExample() {
  return (
    <TextField
      name="email"
      label="Work Email"
      value="dev@example.com"
      placeholder="name@company.com"
      onChange={(value) => console.log(value)}
      id="textfield-example"
      data-testid="textfield-example"
      className="custom-wrapper"
      style={{ marginTop: 8, maxWidth: 480 }}
    />
  );
}
`}
        </RenderSource>
    </FlexDiv>
</FlexDiv>
