import {Meta, Unstyled} from '@storybook/addon-docs/blocks';
import {FlexDiv} from '../FlexDiv';
import * as ModalStories from './Modal.stories';
import {RenderSource} from '../../../documentation/_components/RenderSource/RenderSource';

<Meta of={ModalStories} />

<FlexDiv width={"fill"} height={"fill"} align={"center"}>
    <FlexDiv width={"fill"} maxWidth={760} height={"auto"}>
        <Unstyled>
        # Modal
        Renders a dialog surface with an optional title bar, close action, and footer actions.

        ## Base element
        `HTMLDivElement`

        ## Interface
        `ModalProps`

        ## Props
        <table style={{width: '100%'}}>
          <colgroup>
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '97%'}} />
          </colgroup>
          <thead>
            <tr>
              <th className={"left"}>Prop</th>
              <th className={"left"}>Type</th>
              <th className={"left"}>Default</th>
              <th>Description</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>actions</code></td><td><code>{`ModalAction<T>[]`}</code></td><td><code>-</code></td><td>Optional footer actions rendered on the left or right side.</td></tr>
            <tr><td><code>actionsBorderColor</code></td><td><code>string</code></td><td><code>'var(--core-outline-primary)'</code></td><td>Border color for the actions row.</td></tr>
            <tr><td><code>actionsBorderWidth</code></td><td><code>number</code></td><td><code>1</code></td><td>Border width for the actions row.</td></tr>
            <tr><td><code>backgroundColor</code></td><td><code>string</code></td><td><code>'var(--core-surface-secondary)'</code></td><td>Background color of the modal surface.</td></tr>
            <tr><td><code>borderColor</code></td><td><code>string</code></td><td><code>'var(--core-outline-primary)'</code></td><td>Border color of the modal surface.</td></tr>
            <tr><td><code>borderRadius</code></td><td><code>number | string</code></td><td><code>8</code></td><td>Border radius applied to the modal shell.</td></tr>
            <tr><td><code>borderWidth</code></td><td><code>number | string</code></td><td><code>1</code></td><td>Border width applied to the modal shell.</td></tr>
            <tr><td><code>children</code></td><td><code>ReactNode</code></td><td><code>-</code></td><td>Scrollable content rendered inside the modal body.</td></tr>
            <tr><td><code>close</code></td><td><code>boolean</code></td><td><code>true</code></td><td>Shows the close button in the title bar.</td></tr>
            <tr><td><code>contentSize</code></td><td><code>{`'xs' | 's' | 'm' | 'l' | 'xl'`}</code></td><td><code>'m'</code></td><td>Typography size for the modal content.</td></tr>
            <tr><td><code>maxHeight</code></td><td><code>number | string</code></td><td><code>400</code></td><td>Maximum height of the modal.</td></tr>
            <tr><td><code>maxWidth</code></td><td><code>number | string</code></td><td><code>400</code></td><td>Maximum width of the modal.</td></tr>
            <tr><td><code>minHeight</code></td><td><code>number | string</code></td><td><code>200</code></td><td>Minimum height of the modal.</td></tr>
            <tr><td><code>minWidth</code></td><td><code>number | string</code></td><td><code>300</code></td><td>Minimum width of the modal.</td></tr>
            <tr><td><code>onDragPointerDown</code></td><td><code>{`(e: React.PointerEvent<any>) => void`}</code></td><td><code>-</code></td><td>Optional drag-handle callback used by `ModalController` or custom draggable wrappers.</td></tr>
            <tr><td><code>padding</code></td><td><code>number | string</code></td><td><code>16</code></td><td>Interior spacing token/value used by the title, content, and footer regions.</td></tr>
            <tr><td><code>title</code></td><td><code>string</code></td><td><code>-</code></td><td>Optional title shown in the header row.</td></tr>
            <tr><td><code>titleBorderColor</code></td><td><code>string</code></td><td><code>'var(--core-outline-primary)'</code></td><td>Border color for the title row.</td></tr>
            <tr><td><code>titleBorderWidth</code></td><td><code>number</code></td><td><code>0</code></td><td>Border width for the title row.</td></tr>
            <tr><td><code>titleColor</code></td><td><code>string</code></td><td><code>'var(--core-text-special)'</code></td><td>Text color for the modal title.</td></tr>
            <tr><td><code>titleIcon</code></td><td><code>string</code></td><td><code>-</code></td><td>Optional icon name rendered before the title.</td></tr>
            <tr><td><code>titleSize</code></td><td><code>{`'xs' | 's' | 'm' | 'l' | 'xl'`}</code></td><td><code>'l'</code></td><td>Typography size for the title row.</td></tr>
            <tr><td><code>...divAttributes</code></td><td><code>{`HTMLAttributes<HTMLElement>`}</code></td><td><code>-</code></td><td>Native element props pass through to the modal wrapper, including `id`, `data-testid`, `className`, `style`, and `aria-*` attributes.</td></tr>
          </tbody>
        </table>

        ## Events
        <table style={{width: '100%'}}>
          <colgroup>
            <col style={{width: '1%'}} />
            <col style={{width: '99%'}} />
          </colgroup>
          <thead>
            <tr>
              <th className={"left"}>Event</th>
              <th className={"left"}>Type</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>onAction</code></td><td><code>{`(value?: T) => void`}</code></td></tr>
            <tr><td><code>onClose</code></td><td><code>{`() => void`}</code></td></tr>
            <tr><td><code>onReject</code></td><td><code>{`(error?: Error) => void`}</code></td></tr>
            <tr><td><code>onResolve</code></td><td><code>{`(value?: T) => void`}</code></td></tr>
            <tr><td><code>onToolTip</code></td><td><code>{`(tip: ToolTip | null) => void`}</code></td></tr>
          </tbody>
        </table>

        ## Code Examples
        </Unstyled>

        <RenderSource language={"tsx"}>
```
import { useRef } from 'react';
import { FlexDiv, Modal } from '@apple-pie/slice';

export function ModalExample() {
    return (
        <FlexDiv height={320} justify="center" align="center">
            <Modal
                title="Confirmation"
                actions={[
                    { id: 'cancel', label: 'Cancel', value: 'cancel' },
                    { id: 'confirm', label: 'Continue', value: 'confirm', primary: true },
                ]}
            >
                Are you sure you want to continue?
            </Modal>
        </FlexDiv>
    );
}
```
        </RenderSource>
    </FlexDiv>
</FlexDiv>
