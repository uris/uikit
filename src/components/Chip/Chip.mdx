import {Meta, Unstyled} from '@storybook/addon-docs/blocks';
import {FlexDiv} from '../FlexDiv';
import * as ChipStories from './Chip.stories';
import {RenderSource} from '../../../documentation/_components/RenderSource/RenderSource';

<Meta of={ChipStories} />

<FlexDiv width={"fill"} height={"fill"} alignItems={"center"}>
    <FlexDiv width={"fill"} maxWidth={760} height={"auto"}>
        <Unstyled>
        # Chip
        Renders a compact chip for tags, filters, and selectable metadata items.

        ## Base element
        `HTMLDivElement`

        ## Interface
        `ChipProps`

        ## Props
        <table style={{width: '100%'}}>
          <colgroup>
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '97%'}} />
          </colgroup>
          <thead>
            <tr>
              <th className={"left"}>Prop</th>
              <th className={"left"}>Type</th>
              <th className={"left"}>Default</th>
              <th>Description</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>background</code></td><td><code>string</code></td><td><code>-</code></td><td>Text/value for `background`.</td></tr>
            <tr><td><code>disabled</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `disabled`.</td></tr>
            <tr><td><code>focused</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `focused`.</td></tr>
            <tr><td><code>icon</code></td><td><code>{`IconNames | string`}</code></td><td><code>-</code></td><td>Icon configuration for `icon`.</td></tr>
            <tr><td><code>iconColor</code></td><td><code>string</code></td><td><code>-</code></td><td>Color token/value for `iconColor`.</td></tr>
            <tr><td><code>iconRight</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `iconRight`.</td></tr>
            <tr><td><code>label</code></td><td><code>string</code></td><td><code>-</code></td><td>Text/value for `label`.</td></tr>
            <tr><td><code>labelSize</code></td><td><code>{`'s' | 'm' | 'l'`}</code></td><td><code>-</code></td><td>Sizing/layout setting for `labelSize`.</td></tr>
            <tr><td><code>tooltip</code></td><td><code>string</code></td><td><code>-</code></td><td>Text/value for `tooltip`.</td></tr>
            <tr><td><code>unframed</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `unframed`.</td></tr>
            <tr><td><code>variant</code></td><td><code>{`'small' | 'regular'`}</code></td><td><code>-</code></td><td>Controls `variant`.</td></tr>
            <tr><td><code>...divAttributes</code></td><td><code>{`HTMLAttributes<HTMLDivElement>`}</code></td><td><code>-</code></td><td>Native `div` props pass through (for example `id`, `data-testid`, `className`, `style`, `aria-*`).</td></tr>
          </tbody>
        </table>

        ## Events
        <table style={{width: '100%'}}>
          <colgroup>
            <col style={{width: '1%'}} />
            <col style={{width: '99%'}} />
          </colgroup>
          <thead>
            <tr>
              <th className={"left"}>Event</th>
              <th className={"left"}>Type</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>onToolTip</code></td><td><code>{`(tip: ToolTip | null) => void`}</code></td></tr>
            <tr><td><code>onClick</code></td><td><code>{`(e: React.MouseEvent<HTMLDivElement> | undefined) => void`}</code></td></tr>
            <tr><td><code>onMouseDown</code></td><td><code>{`(e: React.MouseEvent<HTMLDivElement> | undefined) => void`}</code></td></tr>
          </tbody>
        </table>

        ## Code Examples
        </Unstyled>

        <RenderSource language={"tsx"}>
{`
import { Chip } from '@apple-pie/slice';

export function ChipExample() {
  return (
    <Chip
      label="In Review"
      icon="clock"
      variant="regular"
      onClick={() => console.log('clicked')}
      id="chip-example"
      data-testid="chip-example"
      className="custom-wrapper"
      style={{ marginTop: 8, maxWidth: 480 }}
    />
  );
}
`}
        </RenderSource>
    </FlexDiv>
</FlexDiv>
