import {Meta, Unstyled} from '@storybook/addon-docs/blocks';
import {FlexDiv} from '../FlexDiv';
import * as GrouperStories from './Grouper.stories';
import {RenderSource} from '../../../documentation/_components/RenderSource/RenderSource';

<Meta of={GrouperStories} />

<FlexDiv width={"fill"} height={"fill"} align={"center"}>
    <FlexDiv width={"fill"} maxWidth={760} height={"auto"}>
        <Unstyled>
        # Grouper
        Renders a grouped selector row with optional icon, count, and active state handling.

        ## Base element
        `HTMLButtonElement`

        ## Interface
        `GrouperProps`

        ## Props
        <table style={{width: '100%'}}>
          <colgroup>
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '97%'}} />
          </colgroup>
          <thead>
            <tr>
              <th className={"left"}>Prop</th>
              <th className={"left"}>Type</th>
              <th className={"left"}>Default</th>
              <th>Description</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>border</code></td><td><code>number</code></td><td><code>-</code></td><td>Sizing/layout setting for `border`.</td></tr>
            <tr><td><code>count</code></td><td><code>{`number | string`}</code></td><td><code>-</code></td><td>Text/value for `count`.</td></tr>
            <tr><td><code>frameSize</code></td><td><code>number</code></td><td><code>-</code></td><td>Sizing/layout setting for `frameSize`.</td></tr>
            <tr><td><code>hasIcon</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `hasIcon`.</td></tr>
            <tr><td><code>hideNull</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `hideNull`.</td></tr>
            <tr><td><code>iconName</code></td><td><code>string</code></td><td><code>-</code></td><td>Icon configuration for `iconName`.</td></tr>
            <tr><td><code>iconSize</code></td><td><code>number</code></td><td><code>-</code></td><td>Sizing/layout setting for `iconSize`.</td></tr>
            <tr><td><code>open</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `open`.</td></tr>
            <tr><td><code>showFilterBadge</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `showFilterBadge`.</td></tr>
            <tr><td><code>title</code></td><td><code>string</code></td><td><code>-</code></td><td>Text/value for `title`.</td></tr>
            <tr><td><code>toggle</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `toggle`.</td></tr>
            <tr><td><code>unframed</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `unframed`.</td></tr>
            <tr><td><code>variant</code></td><td><code>string</code></td><td><code>-</code></td><td>Text/value for `variant`.</td></tr>
            <tr><td><code>...buttonAttributes</code></td><td><code>{`ButtonHTMLAttributes<HTMLButtonElement>`}</code></td><td><code>-</code></td><td>Native `button` props pass through (for example `id`, `data-testid`, `className`, `style`, `aria-*`, `disabled`).</td></tr>
          </tbody>
        </table>

        ## Events
        <table style={{width: '100%'}}>
          <colgroup>
            <col style={{width: '1%'}} />
            <col style={{width: '99%'}} />
          </colgroup>
          <thead>
            <tr>
              <th className={"left"}>Event</th>
              <th className={"left"}>Type</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>onChange</code></td><td><code>{`(state: boolean) => void`}</code></td></tr>
            <tr><td><code>onClick</code></td><td><code>{`() => void`}</code></td></tr>
          </tbody>
        </table>

        ## Code Examples
        </Unstyled>

        <RenderSource language={"tsx"}>
```
import { Grouper } from '@apple-pie/slice';

export function GrouperExample() {
    return (
        <Grouper
            title="Filters"
            open={true}
            toggle={true}
            count={3}
            id="grouper-example"
            data-testid="grouper-example"
            className="custom-wrapper"
            style={{ marginTop: 8, maxWidth: 480 }}
        />
    );
}
```
        </RenderSource>
    </FlexDiv>
</FlexDiv>
