import {Meta, Unstyled} from '@storybook/addon-docs/blocks';
import {FlexDiv} from '../../FlexDiv';
import * as ProgressIndicatorStories from './ProgressIndicator.stories';
import {RenderSource} from '../../../../documentation/_components/RenderSource/RenderSource';

<Meta of={ProgressIndicatorStories} />

<FlexDiv width={"fill"} height={"fill"} align={"center"}>
    <FlexDiv width={"fill"} maxWidth={760} height={"auto"}>
        <Unstyled>
        # ProgressIndicator
        Renders an animated progress spinner for loading and in-progress states.

        ## Base element
        `HTMLDivElement`

        ## Interface
        `ProgressIndicatorProps`

        ## Props
        <table style={{width: '100%'}}>
          <colgroup>
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '97%'}} />
          </colgroup>
          <thead>
            <tr>
              <th className={"left"}>Prop</th>
              <th className={"left"}>Type</th>
              <th className={"left"}>Default</th>
              <th>Description</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>color</code></td><td><code>string</code></td><td><code>-</code></td><td>Color token/value for `color`.</td></tr>
            <tr><td><code>didStart</code></td><td><code>{`() => void`}</code></td><td><code>-</code></td><td>Controls `didStart`.</td></tr>
            <tr><td><code>didStop</code></td><td><code>{`() => void`}</code></td><td><code>-</code></td><td>Controls `didStop`.</td></tr>
            <tr><td><code>duration</code></td><td><code>number</code></td><td><code>-</code></td><td>Controls `duration`.</td></tr>
            <tr><td><code>inline</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `inline`.</td></tr>
            <tr><td><code>secondsPerSpin</code></td><td><code>number</code></td><td><code>-</code></td><td>Controls `secondsPerSpin`.</td></tr>
            <tr><td><code>show</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `show`.</td></tr>
            <tr><td><code>size</code></td><td><code>number</code></td><td><code>-</code></td><td>Sizing/layout setting for `size`.</td></tr>
            <tr><td><code>stroke</code></td><td><code>number</code></td><td><code>-</code></td><td>Controls `stroke`.</td></tr>
            <tr><td><code>...divAttributes</code></td><td><code>{`HTMLAttributes<HTMLDivElement>`}</code></td><td><code>-</code></td><td>Native `div` props pass through (for example `id`, `data-testid`, `className`, `style`, `aria-*`).</td></tr>
          </tbody>
        </table>

        ## Events
        <table style={{width: '100%'}}>
          <colgroup>
            <col style={{width: '1%'}} />
            <col style={{width: '99%'}} />
          </colgroup>
          <thead>
            <tr>
              <th className={"left"}>Event</th>
              <th className={"left"}>Type</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>-</code></td><td><code>-</code></td></tr>
          </tbody>
        </table>

        ## Code Examples
        </Unstyled>

        <RenderSource language={"tsx"}>
```
import { ProgressIndicator } from '@apple-pie/slice';

export function ProgressIndicatorExample() {
    return (
        <ProgressIndicator
            show={true}
            size={20}
            secondsPerSpin={1.2}
            id="progressindicator-example"
            data-testid="progressindicator-example"
            className="custom-wrapper"
            style={{ marginTop: 8, maxWidth: 480 }}
        />
    );
}
```
        </RenderSource>
    </FlexDiv>
</FlexDiv>
