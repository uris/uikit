import {Meta, Unstyled} from '@storybook/addon-docs/blocks';
import {FlexDiv} from '../FlexDiv';
import * as IconStories from './Icon.stories';
import {RenderSource} from '../../../documentation/_components/RenderSource/RenderSource';

<Meta of={IconStories} />

<FlexDiv width={"fill"} height={"fill"} align={"center"}>
    <FlexDiv width={"fill"} maxWidth={760} height={"auto"}>
        <Unstyled>
        # Icon
        Renders icons from the shared icon registry with configurable size, stroke, and interaction states.

        ## Base element
        `SVGSVGElement`

        ## Interface
        `IconProps`

        ## Props
        <table style={{width: '100%'}}>
          <colgroup>
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '97%'}} />
          </colgroup>
          <thead>
            <tr>
              <th className={"left"}>Prop</th>
              <th className={"left"}>Type</th>
              <th className={"left"}>Default</th>
              <th>Description</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>coverUp</code></td><td><code>string</code></td><td><code>theme.current.colors['core-surface-primary']</code></td><td>Background color used by icons that need to mask underlying strokes.</td></tr>
            <tr><td><code>disabled</code></td><td><code>boolean</code></td><td><code>false</code></td><td>Dims the icon and disables keyboard focus.</td></tr>
            <tr><td><code>fillColor</code></td><td><code>number</code></td><td><code>0.2</code></td><td>Opacity value used for filled portions of supported icons.</td></tr>
            <tr><td><code>name</code></td><td><code>string</code></td><td><code>'home'</code></td><td>Registry name of the icon to render.</td></tr>
            <tr><td><code>pointer</code></td><td><code>boolean</code></td><td><code>false</code></td><td>Shows a pointer cursor and enables keyboard focus when the icon is interactive.</td></tr>
            <tr><td><code>size</code></td><td><code>number</code></td><td><code>20</code></td><td>Rendered icon size in pixels.</td></tr>
            <tr><td><code>stroke</code></td><td><code>number</code></td><td><code>1.5</code></td><td>Stroke width passed to the icon definition.</td></tr>
            <tr><td><code>strokeColor</code></td><td><code>string</code></td><td><code>theme.current.colors['core-icon-primary']</code></td><td>Stroke color token or CSS color value.</td></tr>
            <tr><td><code>toggle</code></td><td><code>boolean</code></td><td><code>false</code></td><td>Switches to the toggled icon variant when the registry entry provides one.</td></tr>
            <tr><td><code>...svgAttributes</code></td><td><code>{`SVGAttributes<SVGSVGElement>`}</code></td><td><code>-</code></td><td>Native `svg` props pass through (for example `id`, `data-testid`, `className`, `style`, `aria-*`).</td></tr>
          </tbody>
        </table>

        ## Events
        <table style={{width: '100%'}}>
          <colgroup>
            <col style={{width: '1%'}} />
            <col style={{width: '99%'}} />
          </colgroup>
          <thead>
            <tr>
              <th className={"left"}>Event</th>
              <th className={"left"}>Type</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>onClick</code></td><td><code>{`(e: React.MouseEvent<SVGElement, MouseEvent>) => void`}</code></td></tr>
          </tbody>
        </table>

        ## Code Examples
        </Unstyled>

        <RenderSource language={"tsx"}>
```
import { Icon } from '@apple-pie/slice';

export function IconExample() {
    return (
        <Icon
            name="home"
            size={20}
            strokeColor="var(--core-icon-primary)"
            onClick={() => console.log('clicked')}
            id="icon-example"
            data-testid="icon-example"
            className="custom-wrapper"
            style={{ display: 'block', marginTop: 8 }}
        />
    );
}
```
        </RenderSource>
    </FlexDiv>
</FlexDiv>
