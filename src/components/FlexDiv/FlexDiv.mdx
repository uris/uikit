import {Meta, Unstyled} from '@storybook/addon-docs/blocks';
import {FlexDiv} from './';
import * as FlexDivStories from './FlexDiv.stories';
import {RenderSource} from '../../../documentation/_components/RenderSource/RenderSource';

<Meta of={FlexDivStories} />

<FlexDiv width={"fill"} height={"fill"} align={"center"}>
    <FlexDiv width={"fill"} maxWidth={760} height={"auto"}>
        <Unstyled>
        # FlexDiv
        Renders a flexbox layout primitive for arranging content with shorthand props for alignment, spacing, and sizing.

        ## Base element
        `HTMLDivElement`

        ## Interface
        `FlexDivProps`

        ## Props
        <table style={{width: '100%'}}>
          <colgroup>
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '97%'}} />
          </colgroup>
          <thead>
            <tr>
              <th className={"left"}>Prop</th>
              <th className={"left"}>Type</th>
              <th className={"left"}>Default</th>
              <th>Description</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>absolute</code></td><td><code>boolean</code></td><td><code>false</code></td><td>Positions the wrapper absolutely and pins it to all four edges.</td></tr>
            <tr><td><code>align</code></td><td><code>{`'start' | 'center' | 'end' | 'between'`}</code></td><td><code>preset-dependent</code></td><td>Controls cross-axis alignment. Explicit values override the selected <code>preset</code>.</td></tr>
            <tr><td><code>animate</code></td><td><code>any</code></td><td><code>-</code></td><td>Controls `animate`.</td></tr>
            <tr><td><code>background</code></td><td><code>string</code></td><td><code>'var(--core-surface-primary)'</code></td><td>Background color or CSS background value for the wrapper.</td></tr>
            <tr><td><code>border</code></td><td><code>string</code></td><td><code>-</code></td><td>CSS border shorthand applied to the wrapper.</td></tr>
            <tr><td><code>borderRadius</code></td><td><code>number</code></td><td><code>-</code></td><td>Border radius in pixels.</td></tr>
            <tr><td><code>centerSelf</code></td><td><code>{`boolean | string`}</code></td><td><code>-</code></td><td>Centers the wrapper horizontally by applying <code>margin: 0 auto</code>.</td></tr>
            <tr><td><code>children</code></td><td><code>any</code></td><td><code>-</code></td><td>Controls `children`.</td></tr>
            <tr><td><code>className</code></td><td><code>string</code></td><td><code>-</code></td><td>Text/value for `className`.</td></tr>
            <tr><td><code>direction</code></td><td><code>{`'row' | 'column'`}</code></td><td><code>preset-dependent</code></td><td>Flex direction for children. Explicit values override the selected <code>preset</code>.</td></tr>
            <tr><td><code>enter</code></td><td><code>any</code></td><td><code>-</code></td><td>Controls `enter`.</td></tr>
            <tr><td><code>exit</code></td><td><code>any</code></td><td><code>-</code></td><td>Controls `exit`.</td></tr>
            <tr><td><code>flex</code></td><td><code>number</code></td><td><code>-</code></td><td>Controls `flex`.</td></tr>
            <tr><td><code>gap</code></td><td><code>number</code></td><td><code>-</code></td><td>Sizing/layout setting for `gap`.</td></tr>
            <tr><td><code>height</code></td><td><code>{`number | 'grow' | 'fill' | 'fit' | 'viewport' | 'auto' | string`}</code></td><td><code>preset-dependent</code></td><td>Wrapper height. Explicit values override the selected <code>preset</code>.</td></tr>
            <tr><td><code>justify</code></td><td><code>{`'start' | 'top' | 'center' | 'end' | 'bottom' | 'between'`}</code></td><td><code>preset-dependent</code></td><td>Controls main-axis alignment. Explicit values override the selected <code>preset</code>.</td></tr>
            <tr><td><code>margin</code></td><td><code>{`number | string`}</code></td><td><code>-</code></td><td>Sizing/layout setting for `margin`.</td></tr>
            <tr><td><code>maxWidth</code></td><td><code>{`number | string`}</code></td><td><code>-</code></td><td>Sizing/layout setting for `maxWidth`.</td></tr>
            <tr><td><code>padding</code></td><td><code>{`number | string`}</code></td><td><code>-</code></td><td>Sizing/layout setting for `padding`.</td></tr>
            <tr><td><code>preset</code></td><td><code>{`'fill-scroll' | 'fill-center' | 'window' | 'row' | 'row-between' | 'fill-column' | 'default' | 'draggable'`}</code></td><td><code>'default'</code></td><td>Applies a predefined layout set for <code>direction</code>, <code>align</code>, <code>justify</code>, <code>width</code>, <code>height</code>, and <code>scrollY</code>.</td></tr>
            <tr><td><code>reverse</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `reverse`.</td></tr>
            <tr><td><code>scrollHandle</code></td><td><code>string</code></td><td><code>-</code></td><td>Text/value for `scrollHandle`.</td></tr>
            <tr><td><code>scrollHandleHover</code></td><td><code>string</code></td><td><code>-</code></td><td>Text/value for `scrollHandleHover`.</td></tr>
            <tr><td><code>scrollX</code></td><td><code>boolean</code></td><td><code>false</code></td><td>Enables horizontal scrolling when content overflows.</td></tr>
            <tr><td><code>scrollY</code></td><td><code>boolean</code></td><td><code>preset-dependent</code></td><td>Enables vertical scrolling when content overflows. Explicit values override the selected <code>preset</code>.</td></tr>
            <tr><td><code>transition</code></td><td><code>Transition</code></td><td><code>-</code></td><td>Controls `transition`.</td></tr>
            <tr><td><code>variants</code></td><td><code>any</code></td><td><code>-</code></td><td>Controls `variants`.</td></tr>
            <tr><td><code>width</code></td><td><code>{`number | 'grow' | 'fill' | 'fit' | 'viewport' | 'auto' | string`}</code></td><td><code>preset-dependent</code></td><td>Wrapper width. Explicit values override the selected <code>preset</code>.</td></tr>
            <tr><td><code>wrap</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `wrap`.</td></tr>
            <tr><td><code>...divAttributes</code></td><td><code>{`HTMLAttributes<HTMLDivElement>`}</code></td><td><code>-</code></td><td>Native `div` props pass through (for example `id`, `data-testid`, `className`, `style`, `aria-*`).</td></tr>
          </tbody>
        </table>

        ## Events
        <table style={{width: '100%'}}>
          <colgroup>
            <col style={{width: '1%'}} />
            <col style={{width: '99%'}} />
          </colgroup>
          <thead>
            <tr>
              <th className={"left"}>Event</th>
              <th className={"left"}>Type</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>-</code></td><td><code>-</code></td></tr>
          </tbody>
        </table>

        ## Code Examples
        </Unstyled>

        <RenderSource language={"tsx"}>
```
import { FlexDiv } from '@apple-pie/slice';

export function FlexDivExample() {
    return (
        <FlexDiv
            preset="fill-column"
            gap={12}
            padding={16}
            background="var(--core-surface-secondary)"
            id="flexdiv-example"
            data-testid="flexdiv-example"
            className="custom-wrapper"
            style={{ marginTop: 8, maxWidth: 480 }}
        >
            <div>Panel content</div>
        </FlexDiv>
    );
}
```
        </RenderSource>
    </FlexDiv>
</FlexDiv>
