import {Meta, Unstyled} from '@storybook/addon-docs/blocks';
import {FlexDiv} from '../FlexDiv';
import * as SliderStories from './Slider.stories';
import {RenderSource} from '../../../documentation/_components/RenderSource/RenderSource';

<Meta of={SliderStories} />

<FlexDiv width={"fill"} height={"fill"} align={"center"}>
    <FlexDiv width={"fill"} maxWidth={760} height={"auto"}>
        <Unstyled>
        # Slider
        Renders a draggable range control for selecting numeric values within min/max bounds.

        ## Base element
        `HTMLDivElement`

        ## Interface
        `SliderProps`

        ## Props
        <table style={{width: '100%'}}>
          <colgroup>
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '97%'}} />
          </colgroup>
          <thead>
            <tr>
              <th className={"left"}>Prop</th>
              <th className={"left"}>Type</th>
              <th className={"left"}>Default</th>
              <th>Description</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>value</code></td><td><code>number</code></td><td><code>25</code></td><td>Current slider value.</td></tr>
            <tr><td><code>scaleMin</code></td><td><code>number</code></td><td><code>0</code></td><td>Minimum selectable value.</td></tr>
            <tr><td><code>scaleMax</code></td><td><code>number</code></td><td><code>100</code></td><td>Maximum selectable value.</td></tr>
            <tr><td><code>width</code></td><td><code>{`number | string`}</code></td><td><code>100</code></td><td>Width of the slider. Numeric values are treated as pixels. Pass a string such as <code>'100%'</code> for responsive layouts.</td></tr>
            <tr><td><code>height</code></td><td><code>{`number | string`}</code></td><td><code>2</code></td><td>Visual thickness of the slider track.</td></tr>
            <tr><td><code>touchHeight</code></td><td><code>{`number | string`}</code></td><td><code>24</code></td><td>Interactive hit area height used for dragging.</td></tr>
            <tr><td><code>trackHeadSize</code></td><td><code>{`number | null`}</code></td><td><code>7</code></td><td>Size of the draggable thumb. Set to <code>0</code> or <code>null</code> to visually hide it.</td></tr>
            <tr><td><code>headColor</code></td><td><code>string</code></td><td><code>var(--core-text-primary)</code></td><td>Thumb color.</td></tr>
            <tr><td><code>trackColor</code></td><td><code>string</code></td><td><code>var(--core-surface-secondary)</code></td><td>Background track color.</td></tr>
            <tr><td><code>progressColor</code></td><td><code>string</code></td><td><code>var(--core-text-primary)</code></td><td>Filled progress color.</td></tr>
            <tr><td><code>cursor</code></td><td><code>{`'default' | 'grab' | 'grabbing' | 'pointer'`}</code></td><td><code>default</code></td><td>Cursor shown while hovering or dragging the control.</td></tr>
            <tr><td><code>...divAttributes</code></td><td><code>{`HTMLAttributes<HTMLDivElement>`}</code></td><td><code>-</code></td><td>Native `div` props pass through (for example `id`, `data-testid`, `className`, `style`, `aria-*`).</td></tr>
          </tbody>
        </table>

        ## Events
        <table style={{width: '100%'}}>
          <colgroup>
            <col style={{width: '1%'}} />
            <col style={{width: '99%'}} />
          </colgroup>
          <thead>
            <tr>
              <th className={"left"}>Event</th>
              <th className={"left"}>Type</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>onChange</code></td><td><code>{`(value: number, percent: number) => void`}</code></td></tr>
            <tr><td><code>onDragChange</code></td><td><code>{`(value: number, percent: number) => void`}</code></td></tr>
          </tbody>
        </table>

        ## Code Examples
        </Unstyled>

        <RenderSource language={"tsx"}>
```
import { Slider } from '@apple-pie/slice';

export function SliderExample() {
    return (
        <Slider
            value={42}
            scaleMin={0}
            scaleMax={100}
            onChange={(value) => console.log(value)}
            id="slider-example"
            data-testid="slider-example"
            className="custom-wrapper"
            style={{ marginTop: 8, maxWidth: 480 }}
        />
    );
}
```
        </RenderSource>
    </FlexDiv>
</FlexDiv>
