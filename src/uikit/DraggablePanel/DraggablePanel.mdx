import {Meta, Unstyled} from '@storybook/addon-docs/blocks';
import {FlexDiv} from '../FlexDiv';
import * as DraggablePanelStories from './DraggablePanel.stories';
import {RenderSource} from '../../../.storybook/RenderSource';

<Meta of={DraggablePanelStories} />

<FlexDiv width={"fill"} height={"fill"} alignItems={"center"}>
    <FlexDiv width={"fill"} maxWidth={760} height={"auto"}>
        <Unstyled>
        # DraggablePanel
        Renders a resizable, draggable panel container for sidebars and split-view layouts.

        ## Base element
        `HTMLDivElement`

        ## Interface
        `DraggablePanelProps`

        ## Props
        <table style={{width: '100%'}}>
          <colgroup>
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '97%'}} />
          </colgroup>
          <thead>
            <tr>
              <th className={"left"}>Prop</th>
              <th className={"left"}>Type</th>
              <th className={"left"}>Default</th>
              <th>Description</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>...divAttributes</code></td><td><code>{`HTMLAttributes<HTMLDivElement>`}</code></td><td><code>-</code></td><td>Native `div` props pass through (for example `id`, `data-testid`, `className`, `style`, `aria-*`).</td></tr>
          </tbody>
        </table>

        ## Events
        <table style={{width: '100%'}}>
          <colgroup>
            <col style={{width: '1%'}} />
            <col style={{width: '99%'}} />
          </colgroup>
          <thead>
            <tr>
              <th className={"left"}>Event</th>
              <th className={"left"}>Type</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>-</code></td><td><code>-</code></td></tr>
          </tbody>
        </table>

        ## Code Examples
        </Unstyled>

        <RenderSource language={"tsx"}>
{`
import { DraggablePanel } from '@apple-pie/slice';

export function DraggablePanelExample() {
  return (
    <DraggablePanel
      sizeConstraints={{ initial: 320, min: 240, max: 560 }}
      dragsRight={true}
      bgColor="var(--core-surface-secondary)"
      id="draggablepanel-example"
      data-testid="draggablepanel-example"
      className="custom-wrapper"
      style={{ marginTop: 8, height: 240 }}
    >
      <div style={{ padding: 16 }}>Resizable panel content</div>
    </DraggablePanel>
  );
}
`}
        </RenderSource>
    </FlexDiv>
</FlexDiv>
