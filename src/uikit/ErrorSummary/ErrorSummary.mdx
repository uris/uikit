import {Meta, Unstyled} from '@storybook/addon-docs/blocks';
import {FlexDiv} from '../FlexDiv';
import * as ErrorSummaryStories from './ErrorSummary.stories';
import {RenderSource} from '../../../.storybook/RenderSource';

<Meta of={ErrorSummaryStories} />

<FlexDiv width={"fill"} height={"fill"} alignItems={"center"}>
    <FlexDiv width={"fill"} maxWidth={760} height={"auto"}>
        <Unstyled>
        # ErrorSummary
        ErrorSummary component documentation.

        ## Base element
        `HTMLDivElement`

        ## Interface
        `ErrorSummaryProps`

        ## Props
        <table style={{width: '100%'}}>
          <colgroup>
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '97%'}} />
          </colgroup>
          <thead>
            <tr>
              <th className={"left"}>Prop</th>
              <th className={"left"}>Type</th>
              <th className={"left"}>Default</th>
              <th>Description</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>entries</code></td><td><code>ErrorMessage[]</code></td><td><code>-</code></td><td>Controls `entries`.</td></tr>
            <tr><td><code>errors</code></td><td><code>any[]</code></td><td><code>-</code></td><td>Controls `errors`.</td></tr>
            <tr><td><code>...divAttributes</code></td><td><code>{`HTMLAttributes<HTMLDivElement>`}</code></td><td><code>-</code></td><td>Native `div` props pass through (for example `id`, `data-testid`, `className`, `style`, `aria-*`).</td></tr>
          </tbody>
        </table>

        ## Events
        <table style={{width: '100%'}}>
          <colgroup>
            <col style={{width: '1%'}} />
            <col style={{width: '99%'}} />
          </colgroup>
          <thead>
            <tr>
              <th className={"left"}>Event</th>
              <th className={"left"}>Type</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>-</code></td><td><code>-</code></td></tr>
          </tbody>
        </table>

        ## Code Examples
        </Unstyled>

        <RenderSource language={"tsx"}>
{`
import { ErrorSummary } from '@apple-pie/slice';

export function ErrorSummaryExample() {
  return (
    <ErrorSummary
      entries={[{ id: 'name', title: 'Missing required field', bullets: ['Name is required'] }]}
      id="errorsummary-example"
      data-testid="errorsummary-example"
      className="custom-wrapper"
      style={{ marginTop: 8, maxWidth: 480 }}
    />
  );
}
`}
        </RenderSource>
    </FlexDiv>
</FlexDiv>
