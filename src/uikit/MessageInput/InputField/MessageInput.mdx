import {Meta, Unstyled} from '@storybook/addon-docs/blocks';
import {FlexDiv} from '../../FlexDiv';
import * as MessageInputStories from './MessageInput.stories';
import {RenderSource} from '../../../../.storybook/RenderSource';

<Meta of={MessageInputStories} />

<FlexDiv width={"fill"} height={"fill"} alignItems={"center"}>
    <FlexDiv width={"fill"} maxWidth={760} height={"auto"}>
        <Unstyled>
        # MessageInput
        MessageInput component documentation.

        ## Base element
        `HTMLDivElement`

        ## Interface
        `MessageInputProps`

        ## Props
        <table style={{width: '100%'}}>
          <colgroup>
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '97%'}} />
          </colgroup>
          <thead>
            <tr>
              <th className={"left"}>Prop</th>
              <th className={"left"}>Type</th>
              <th className={"left"}>Default</th>
              <th>Description</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>attachClick</code></td><td><code>{`(e: React.MouseEvent<any> | undefined) => void`}</code></td><td><code>-</code></td><td>Controls `attachClick`.</td></tr>
            <tr><td><code>complianceCheckClick</code></td><td><code>{`() => void`}</code></td><td><code>-</code></td><td>Controls `complianceCheckClick`.</td></tr>
            <tr><td><code>currentUser</code></td><td><code>string</code></td><td><code>-</code></td><td>Text/value for `currentUser`.</td></tr>
            <tr><td><code>error</code></td><td><code>{`string | null`}</code></td><td><code>-</code></td><td>Text/value for `error`.</td></tr>
            <tr><td><code>excerpts</code></td><td><code>DocExcerpt[]</code></td><td><code>-</code></td><td>Controls `excerpts`.</td></tr>
            <tr><td><code>files</code></td><td><code>File[]</code></td><td><code>-</code></td><td>Controls `files`.</td></tr>
            <tr><td><code>focused</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `focused`.</td></tr>
            <tr><td><code>height</code></td><td><code>string</code></td><td><code>-</code></td><td>Sizing/layout setting for `height`.</td></tr>
            <tr><td><code>isFetching</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `isFetching`.</td></tr>
            <tr><td><code>isShort</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `isShort`.</td></tr>
            <tr><td><code>isStreaming</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `isStreaming`.</td></tr>
            <tr><td><code>jurisdiction</code></td><td><code>{`JurisdictionFocus | null`}</code></td><td><code>-</code></td><td>Controls `jurisdiction`.</td></tr>
            <tr><td><code>jurisdictionClick</code></td><td><code>{`() => void`}</code></td><td><code>-</code></td><td>Controls `jurisdictionClick`.</td></tr>
            <tr><td><code>maxHeight</code></td><td><code>number</code></td><td><code>-</code></td><td>Sizing/layout setting for `maxHeight`.</td></tr>
            <tr><td><code>owner</code></td><td><code>string</code></td><td><code>-</code></td><td>Text/value for `owner`.</td></tr>
            <tr><td><code>placeholder</code></td><td><code>string</code></td><td><code>-</code></td><td>Text/value for `placeholder`.</td></tr>
            <tr><td><code>presenceID</code></td><td><code>string</code></td><td><code>-</code></td><td>Text/value for `presenceID`.</td></tr>
            <tr><td><code>role</code></td><td><code>string</code></td><td><code>-</code></td><td>Text/value for `role`.</td></tr>
            <tr><td><code>showFilters</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `showFilters`.</td></tr>
            <tr><td><code>users</code></td><td><code>UserPresence[]</code></td><td><code>-</code></td><td>Controls `users`.</td></tr>
            <tr><td><code>value</code></td><td><code>string</code></td><td><code>-</code></td><td>Text/value for `value`.</td></tr>
            <tr><td><code>...divAttributes</code></td><td><code>{`HTMLAttributes<HTMLDivElement>`}</code></td><td><code>-</code></td><td>Native `div` props pass through (for example `id`, `data-testid`, `className`, `style`, `aria-*`).</td></tr>
          </tbody>
        </table>

        ## Events
        <table style={{width: '100%'}}>
          <colgroup>
            <col style={{width: '1%'}} />
            <col style={{width: '99%'}} />
          </colgroup>
          <thead>
            <tr>
              <th className={"left"}>Event</th>
              <th className={"left"}>Type</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>onChangeFiles</code></td><td><code>{`(files: File[]) => void`}</code></td></tr>
            <tr><td><code>onChangeExcerpts</code></td><td><code>{`(excerpts: DocExcerpt[]) => void`}</code></td></tr>
            <tr><td><code>onTogglePrompt</code></td><td><code>{`(presence: UserPresence) => void`}</code></td></tr>
            <tr><td><code>onToolTip</code></td><td><code>{`(tip: ToolTip | null) => void`}</code></td></tr>
            <tr><td><code>onChange</code></td><td><code>{`(prompt: string) => void`}</code></td></tr>
            <tr><td><code>onBlur</code></td><td><code>{`() => void`}</code></td></tr>
            <tr><td><code>onFocus</code></td><td><code>{`() => void`}</code></td></tr>
            <tr><td><code>onSend</code></td><td><code>{`(message: SendMessage) => void`}</code></td></tr>
            <tr><td><code>onStop</code></td><td><code>{`() => void`}</code></td></tr>
          </tbody>
        </table>

        ## Code Examples
        </Unstyled>

        <RenderSource language={"tsx"}>
{`
import { MessageInput } from '@apple-pie/slice';

export function MessageInputExample() {
  return (
    <MessageInput
      placeholder="Ask a compliance question"
      value="How should we handle overtime approvals?"
      currentUser="dev@example.com"
      owner="owner@example.com"
      files={[]}
      excerpts={[{ docTitle: 'Employee Handbook', content: 'Overtime policy excerpt' }]}
      users={[{ first: 'Dev', last: 'User', email: 'dev@example.com' }]}
      onChange={(prompt) => console.log(prompt)}
      onSend={(message) => console.log(message)}
      id="messageinput-example"
      data-testid="messageinput-example"
      className="custom-wrapper"
      style={{ marginTop: 8, maxWidth: 480 }}
    />
  );
}
`}
        </RenderSource>
    </FlexDiv>
</FlexDiv>
