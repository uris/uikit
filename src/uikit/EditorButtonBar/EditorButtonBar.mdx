import {Meta, Unstyled} from '@storybook/addon-docs/blocks';
import {FlexDiv} from '../FlexDiv';
import * as EditorButtonBarStories from './EditorButtonBar.stories';

<Meta of={EditorButtonBarStories} />

<FlexDiv width={"fill"} height={"fill"} alignItems={"center"}>
    <FlexDiv width={"fill"} maxWidth={760} height={"auto"}>
        <Unstyled>
        # EditorButtonBar

        Part of `Components/EditorButtonBar`.

        ## Purpose

        - Use `EditorButtonBar` when you need this UI primitive inside the Slice component system.
        - Implementation reviewed from: `src/uikit/EditorButtonBar/EditorButtonBar.tsx`.

        ## Story Variants

        - `Default`

        ## Attributes

        - Props interface: `EditorButtonBarProps`

        | Prop | Type | Required | Default | Notes |
        | --- | --- | --- | --- | --- |
        | `onCommand` | `(command: any, e: React.MouseEvent<any> \| undefined) => void` | No | `() => null` | callback |
        | `onToolTip` | `(tip: ToolTip \| null) => void` | No | `() => null` | callback |
        | `shortSize` | `number` | No | `500` | number |
        | `mediumSize` | `number` | No | `664` | number |
        | `state` | `'small' \\| 'medium' \\| 'regular' \\| 'auto'` | No | `'auto'` | union |
        | `activeFormats` | `string[]` | No | `[]` | array |
        | `activeStyle` | `'h1' \\| 'h2' \\| 'h3' \\| 'p'` | No | `'p'` | union |
        | `disabledFormats` | `string[]` | No | `[]` | array |
        </Unstyled>
    </FlexDiv>
</FlexDiv>
