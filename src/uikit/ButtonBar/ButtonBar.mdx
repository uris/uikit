import {Meta, Unstyled} from '@storybook/addon-docs/blocks';
import {FlexDiv} from '../FlexDiv';
import * as ButtonBarStories from './ButtonBar.stories';
import {RenderSource} from '../../../.storybook/RenderSource';

<Meta of={ButtonBarStories} />

<FlexDiv width={"fill"} height={"fill"} alignItems={"center"}>
    <FlexDiv width={"fill"} maxWidth={760} height={"auto"}>
        <Unstyled>
        # ButtonBar
        Renders a segmented row of icon buttons and emits the selected command option.

        ## Base element
        `HTMLDivElement`

        ## Interface
        `ButtonBarProps`

        ## Props
        <table style={{width: '100%'}}>
          <colgroup>
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '97%'}} />
          </colgroup>
          <thead>
            <tr>
              <th className={"left"}>Prop</th>
              <th className={"left"}>Type</th>
              <th className={"left"}>Default</th>
              <th>Description</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>current</code></td><td><code>number</code></td><td><code>-</code></td><td>Controls `current`.</td></tr>
            <tr><td><code>label</code></td><td><code>string</code></td><td><code>-</code></td><td>Text/value for `label`.</td></tr>
            <tr><td><code>options</code></td><td><code>BarButton[]</code></td><td><code>-</code></td><td>Controls `options`.</td></tr>
            <tr><td><code>...divAttributes</code></td><td><code>{`HTMLAttributes<HTMLDivElement>`}</code></td><td><code>-</code></td><td>Native `div` props pass through (for example `id`, `data-testid`, `className`, `style`, `aria-*`).</td></tr>
          </tbody>
        </table>

        ## Events
        <table style={{width: '100%'}}>
          <colgroup>
            <col style={{width: '1%'}} />
            <col style={{width: '99%'}} />
          </colgroup>
          <thead>
            <tr>
              <th className={"left"}>Event</th>
              <th className={"left"}>Type</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>onChange</code></td><td><code>{`(option: BarButton) => void`}</code></td></tr>
            <tr><td><code>onToolTip</code></td><td><code>{`(tip: ToolTip | null) => void`}</code></td></tr>
          </tbody>
        </table>

        ## Code Examples
        </Unstyled>

        <RenderSource language={"tsx"}>
{`
import { ButtonBar } from '@apple-pie/slice';

export function ButtonBarExample() {
  return (
    <ButtonBar
      label="Formatting"
      options={[{ icon: 'bold', command: 'bold', tip: 'Bold' }, { icon: 'italic', command: 'italic', tip: 'Italic' }]}
      onChange={(option) => console.log(option)}
      id="buttonbar-example"
      data-testid="buttonbar-example"
      className="custom-wrapper"
      style={{ marginTop: 8, maxWidth: 480 }}
    />
  );
}
`}
        </RenderSource>
    </FlexDiv>
</FlexDiv>
