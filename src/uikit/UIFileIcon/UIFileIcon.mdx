import {Meta, Unstyled} from '@storybook/addon-docs/blocks';
import {FlexDiv} from '../FlexDiv';
import * as UIFileIconStories from './UIFileIcon.stories';
import {RenderSource} from '../../../.storybook/RenderSource';

<Meta of={UIFileIconStories} />

<FlexDiv width={"fill"} height={"fill"} alignItems={"center"}>
    <FlexDiv width={"fill"} maxWidth={760} height={"auto"}>
        <Unstyled>
        # UIFileIcon
        UIFileIcon component documentation.

        ## Base element
        `SVGSVGElement`

        ## Interface
        `UIFileIconProps`

        ## Props
        <table style={{width: '100%'}}>
          <colgroup>
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '97%'}} />
          </colgroup>
          <thead>
            <tr>
              <th className={"left"}>Prop</th>
              <th className={"left"}>Type</th>
              <th className={"left"}>Default</th>
              <th>Description</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>disabled</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `disabled`.</td></tr>
            <tr><td><code>name</code></td><td><code>{`UIFileIcons | string`}</code></td><td><code>-</code></td><td>Text/value for `name`.</td></tr>
            <tr><td><code>pointer</code></td><td><code>boolean</code></td><td><code>-</code></td><td>Boolean flag controlling `pointer`.</td></tr>
            <tr><td><code>size</code></td><td><code>number</code></td><td><code>-</code></td><td>Sizing/layout setting for `size`.</td></tr>
            <tr><td><code>...svgAttributes</code></td><td><code>{`SVGAttributes<SVGSVGElement>`}</code></td><td><code>-</code></td><td>Native `svg` props pass through (for example `id`, `data-testid`, `className`, `style`, `aria-*`).</td></tr>
          </tbody>
        </table>

        ## Events
        <table style={{width: '100%'}}>
          <colgroup>
            <col style={{width: '1%'}} />
            <col style={{width: '99%'}} />
          </colgroup>
          <thead>
            <tr>
              <th className={"left"}>Event</th>
              <th className={"left"}>Type</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>onClick</code></td><td><code>{`(e: React.MouseEvent<SVGElement, MouseEvent>) => void`}</code></td></tr>
          </tbody>
        </table>

        ## Code Examples
        </Unstyled>

        <RenderSource language={"tsx"}>
{`
import { UIFileIcon } from '@apple-pie/slice';

export function UIFileIconExample() {
  return (
    <UIFileIcon
      name="pdf"
      size={28}
      onClick={() => console.log('clicked')}
      id="uifileicon-example"
      data-testid="uifileicon-example"
      className="custom-wrapper"
      style={{ display: 'block', marginTop: 8 }}
    />
  );
}
`}
        </RenderSource>
    </FlexDiv>
</FlexDiv>
