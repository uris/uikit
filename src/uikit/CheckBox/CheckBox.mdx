import {Meta, Unstyled} from '@storybook/addon-docs/blocks';
import {FlexDiv} from '../FlexDiv';
import * as CheckBoxStories from './CheckBox.stories';

<Meta of={CheckBoxStories} />

<FlexDiv width={"fill"} height={"fill"} alignItems={"center"}>
    <FlexDiv width={"fill"} maxWidth={760} height={"auto"}>
        <Unstyled>
        # CheckBox

        Part of `Components/CheckBox`.

        ## Purpose

        - Use `CheckBox` when you need this UI primitive inside the Slice component system.
        - Implementation reviewed from: `src/uikit/CheckBox/CheckBox.tsx`.

        ## Story Variants

        - `Default`
        - `Partial`
        - `Disabled`

        ## Attributes

        - Props interface: `CheckBoxProps`

        | Prop | Type | Required | Default | Notes |
        | --- | --- | --- | --- | --- |
        | `onChange` | `(state: boolean) => void` | No | `() => null` | callback |
        | `size` | `number` | No | `20` | number |
        | `checked` | `'mixed' \\| boolean` | No | `false` | union |
        | `disabled` | `boolean` | No | `false` | boolean |
        | `color` | `string` | No | `undefined` | string |
        | `label` | `string` | No | `undefined` | string |
        </Unstyled>
    </FlexDiv>
</FlexDiv>
