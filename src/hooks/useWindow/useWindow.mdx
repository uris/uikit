import {Meta, Unstyled} from '@storybook/addon-docs/blocks';
import {RenderSource} from '../../../documentation/_components/RenderSource/RenderSource';
import {FlexDiv} from '../../components/FlexDiv';
import {Spacer} from "../../components/Spacer";
import Notice from "../../../documentation/_components/Notice/Notice";

<Meta title="Hooks/useWindow" />

<FlexDiv width={"fill"} height={"fill"} alignItems={"center"}>
    <FlexDiv width={"fill"} maxWidth={760} height={"auto"}>
        <Unstyled>
        # useWindow
        Tracks viewport size, form factor, touch capability, pixel ratio, Electron runtime state, and optional geolocation requests.

        ## Signature
        `useWindow(breakpoints?: BreakPoints, top?: boolean)`

        ## Parameters
        <table style={{width: '100%'}}>
            <colgroup>
                <col style={{width: '1%'}} />
                <col style={{width: '1%'}} />
                <col style={{width: '1%'}} />
                <col style={{width: '97%'}} />
            </colgroup>
            <thead>
            <tr>
                <th>Parameter</th>
                <th>Type</th>
                <th>Default</th>
                <th>Description</th>
            </tr>
            </thead>
            <tbody>
            <tr><td><code>breakpoints?</code></td><td><code>BreakPoints</code></td><td><code>{`{ mobile: 576, tablet: 768, desktop: 992, desktopL: 1200, desktopXL: 1400 }`}</code></td><td>Optional width thresholds used to resolve <code>formFactor</code>. If omitted, the hook uses its built-in defaults (Bootstrap 5 breakpoints).</td></tr>
            <tr><td><code>top?</code></td><td><code>boolean</code></td><td><code>true</code></td><td>Use the top most window in the window hierarchy. If used in an iFrame that's on a different domain, it will fall back to that iFrame window.</td></tr>
            </tbody>
        </table>

        ## Returns
        <table style={{width: '100%'}}>
          <colgroup>
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '97%'}} />
          </colgroup>
          <thead>
            <tr>
              <th>Return</th>
              <th>Type</th>
              <th>Default</th>
              <th>Description</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>viewportWidth</code></td><td><code>number | null</code></td><td><code>null</code></td><td>Current `innerWidth`.</td></tr>
            <tr><td><code>viewportHeight</code></td><td><code>number | null</code></td><td><code>null</code></td><td>Current `innerHeight`.</td></tr>
            <tr><td><code>height</code></td><td><code>string</code></td><td><code>'100vh'</code></td><td>A height token for setting layout height.</td></tr>
            <tr><td><code>isTouchDevice</code></td><td><code>boolean</code></td><td><code>false</code></td><td>`true` when touch input is available.</td></tr>
            <tr><td><code>isElectron</code></td><td><code>boolean</code></td><td><code>false</code></td><td>`true` when running in Electron.</td></tr>
            <tr><td><code>isAppleDevice</code></td><td><code>boolean</code></td><td><code>false</code></td><td>`true` when running in iOS/MacOS.</td></tr>
            <tr><td><code>formFactor</code></td><td><code>FormFactor</code></td><td><code>FormFactor.Desktop</code></td><td>Screen classification based on width breakpoints.</td></tr>
            <tr><td><code>dpr</code></td><td><code>number</code></td><td><code>1</code></td><td>Device pixel ratio clamped to `1 | 2 | 3`.</td></tr>
            <tr><td><code>geolocationSupported</code></td><td><code>boolean</code></td><td><code>false</code></td><td>`true` when the browser exposes the Geolocation API.</td></tr>
            <tr><td><code>location</code></td><td><code>WindowGeolocation | null</code></td><td><code>null</code></td><td>The last successfully resolved location returned by <code>requestGeolocation()</code>.</td></tr>
            <tr><td><code>locationError</code></td><td><code>Error | null</code></td><td><code>null</code></td><td>The last geolocation error, including permission denial and timeout failures.</td></tr>
            <tr><td><code>gettingLocation</code></td><td><code>boolean</code></td><td><code>false</code></td><td>`true` while a geolocation lookup is in progress.</td></tr>
            <tr><td><code>requestGeolocation</code></td><td><code>() =&gt; void</code></td><td><code>-</code></td><td>Triggers an explicit browser geolocation request. This does not run automatically, so callers control when the permission prompt appears.</td></tr>
          </tbody>
        </table>

        ## Code Examples
        </Unstyled>

        <RenderSource language={"tsx"}>
        ```
        import { useWindow } from '@apple-pie/slice';
        
        function ViewportBadge() {
            const win = useWindow();
        
            return (
                <div
                    style={{ padding: 12, border: '1px solid var(--core-outline-primary)' }}
                >
                    {win.formFactor} • {win.viewportWidth} x {win.viewportHeight}
                </div>
            );
        }
        ```
        </RenderSource>

        <RenderSource language={"tsx"}>
        ```
        import { FormFactor, useWindow, type BreakPoints } from '@apple-pie/slice';
        
        const customBreakpoints: BreakPoints = {
            [FormFactor.Mobile]: 600,
            [FormFactor.Tablet]: 1024,
            [FormFactor.Desktop]: 1280,
            [FormFactor.DesktopL]: 1536,
            [FormFactor.DesktopXL]: 1920,
        };
        
        function ViewportBadgeWithCustomBreakpoints() {
            const win = useWindow(customBreakpoints);
        
            return (
                <div
                    style={{ padding: 12, border: '1px solid var(--core-outline-primary)' }}
                >
                    {win.formFactor} • {win.viewportWidth} x {win.viewportHeight}
                </div>
            );
        }
        ```
        </RenderSource>

        <RenderSource language={"tsx"}>
        ```
        import { useWindow } from '@apple-pie/slice';
        
        function GeoLocationButton() {
          const win = useWindow();
        
          return (
            <button
              type="button"
              onClick={win.requestGeolocation}
              disabled={!win.geolocationSupported || win.gettingLocation}
            >
              {win.gettingLocation
                ? 'Requesting location...'
                : win.location
                  ? \`\${win.location.latitude}, \${win.location.longitude}\`
                  : 'Request location'}
            </button>
          );
        }
        ```
        </RenderSource>
        <Spacer size={32} />
        <Notice>
            Note: Slice also implements and alternative (but optional) [`WindowStore`](/docs/stores-window-store--docs) that creates a Zustand singleton window state object you can efficiently access throughout your application.
        </Notice>
        <Spacer size={64} />
    </FlexDiv>
</FlexDiv>
