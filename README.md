### Peer Dependencies
You will need to manage your own version of react, react-dom,
styled-components and framer-motion. Your project must have
the following dependencies installed:

```bash
npm install react react-dom styled-components framer-motion