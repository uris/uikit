import { divInputConfig } from '../configs/all-configs';
import { registerBenchConfig } from '../utils/registerBenchConfig';

registerBenchConfig(divInputConfig);

export const divInputBenchmarkConfig = divInputConfig;
