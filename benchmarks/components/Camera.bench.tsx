import { cameraConfig } from '../configs/all-configs';
import { registerBenchConfig } from '../utils/registerBenchConfig';

registerBenchConfig(cameraConfig);

export const cameraBenchmarkConfig = cameraConfig;
