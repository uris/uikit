import { fileIconConfig } from '../configs/all-configs';
import { registerBenchConfig } from '../utils/registerBenchConfig';

registerBenchConfig(fileIconConfig);

export const fileIconBenchmarkConfig = fileIconConfig;
