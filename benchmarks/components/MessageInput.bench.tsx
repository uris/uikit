import { messageInputConfig } from '../configs/all-configs';
import { registerBenchConfig } from '../utils/registerBenchConfig';

registerBenchConfig(messageInputConfig);

export const messageInputBenchmarkConfig = messageInputConfig;
