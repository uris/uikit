import { uiButtonConfig } from '../configs/all-configs';
import { registerBenchConfig } from '../utils/registerBenchConfig';

registerBenchConfig(uiButtonConfig);

export const buttonBenchmarkConfig = uiButtonConfig;
