import { iconButtonConfig } from '../configs/all-configs';
import { registerBenchConfig } from '../utils/registerBenchConfig';

registerBenchConfig(iconButtonConfig);

export const iconButtonBenchmarkConfig = iconButtonConfig;
