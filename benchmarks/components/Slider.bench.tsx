import { sliderConfig } from '../configs/all-configs';
import { registerBenchConfig } from '../utils/registerBenchConfig';

registerBenchConfig(sliderConfig);

export const sliderBenchmarkConfig = sliderConfig;
