import { radioButtonConfig } from '../configs/all-configs';
import { registerBenchConfig } from '../utils/registerBenchConfig';

registerBenchConfig(radioButtonConfig);

export const radioButtonBenchmarkConfig = radioButtonConfig;
