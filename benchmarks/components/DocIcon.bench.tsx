import { docIconConfig } from '../configs/all-configs';
import { registerBenchConfig } from '../utils/registerBenchConfig';

registerBenchConfig(docIconConfig);

export const docIconBenchmarkConfig = docIconConfig;
