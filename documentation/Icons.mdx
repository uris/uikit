import { Meta, Unstyled } from '@storybook/addon-docs/blocks';
import { FlexDiv, Spacer } from 'src';
import {IconSampler} from "./_components/IconSampler/IconSampler";

<Meta title="Icons/Slice Icons" />

<FlexDiv width={"fill"} height={"fill"} align={"center"}>
    <FlexDiv width={"fill"} maxWidth={760} height={"auto"}>
        <Unstyled>
            # Slice Icons
            Slice currently has a limited set of UI icons you can pull from. These will be expanded/grown in upcoming releases.
            
            Icons strive to provide both greater clarity to your UI but also an element of character.
            
            All icons are created with a base dimension of 20px x 20px with a 1.5px stroke having been created to render legibly in sizes as small as 14px. They have both circular, softer edges as well as hard rectangular edges. This makes the icon set a great compliment to the base Funnel Sans type face.

            The shared registry now also includes `keyboard`, `speaker high`, `speaker low`, `talk`, and `sidebar split` alongside the existing UI, media, status, and action icons.
            
            <IconSampler size={64} />
            

            <Spacer size={64} />
        </Unstyled>
    </FlexDiv>
</FlexDiv>
