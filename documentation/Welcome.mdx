import { Meta, Unstyled } from '@storybook/addon-docs/blocks';
import {FlexDiv} from "../src";
import {Spacer} from "../src";
import {RenderSource} from "./_components/RenderSource/RenderSource";
import {Label} from "../src";
import {Logo} from "./_components/Images/Logo";

<Meta title="Welcome/Welcome" />

<FlexDiv width={"fill"} height={"fill"} justify={"start"} alignItems={"center"}>
    <FlexDiv width={"fill"} maxWidth={760} height={"auto"}>
        <FlexDiv width={"fill"} alignItems={"center"}><Logo width={"80%"} /></FlexDiv>
        <Spacer size={32} />
        <Unstyled>
            ✌️Welcome to **Slice** <Label>Beta</Label> - a free, flexible, and fast ui-kit for creating modern react apps (v18+). Start by adding it to your project:
        </Unstyled>
        <RenderSource language='bash'>
```
$ npm install @apple-pie/slice
```
</RenderSource>
        <Unstyled>
            ##### Dependencies
            Folks at [motion.dev](https://motion.dev) created an amazing animation library - Slice uses their work to do some fancy stuff. Make sure that's also in your project.
        </Unstyled>
        <RenderSource language='bash'>
```
$ npm install motion/react
```
</RenderSource>
        <Unstyled>
            That should do it. Let's grab a slice ...
            <Spacer size={32} />
            <p className={"note"}>Note: Slice exposes optional Zustand-based store helpers (for example multi-file upload manager with progress). You only need to install `zustand` if you use these store paths. If you do not import stores, your bundle will tree-shake store code out.</p>
        </Unstyled>
    </FlexDiv>
</FlexDiv>
