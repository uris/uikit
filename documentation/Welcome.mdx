import { Meta, Unstyled } from '@storybook/addon-docs/blocks';
import {FlexDiv} from "../src/components/FlexDiv";
import {Spacer} from "../src/components/Spacer";
import {RenderSource} from "./_components/RenderSource/RenderSource";
import {Label} from "../src/components/Label";

<Meta title="Welcome/Welcome" />

<FlexDiv width={"fill"} height={"fill"} justify={"start"} alignItems={"center"}>
    <FlexDiv width={"fill"} maxWidth={760} height={"auto"}>
        <Unstyled>
            ✌️Welcome to **Slice** <Label inline>Beta</Label> - a free, flexible, and fast ui-kit for creating modern react apps (v18+). Start by adding it to your project:
        </Unstyled>
        <RenderSource language='bash'>
            ```
            $ npm install @apple-pie/slice
            ```
        </RenderSource>
        <Unstyled>
            ##### Dependencies
            Folks at [motion.dev](https://motion.dev) created an amazing animation library - Slice uses their work to do some fancy stuff. Make sure that's also in your project.
        </Unstyled>
        <RenderSource language='bash'>
            ```
            $ npm install motion/react
            ```
        </RenderSource>
        <Unstyled>
            That should do it. Let's grab a slice ...
            <Spacer size={32} />
            <p className={"note"}>Note: Slice exposes optional Zustand-based store helpers (for example toast state). You only need to install `zustand` if you use these store paths. If you do not import stores, your app bundle can tree-shake store code out.</p>
        </Unstyled>
    </FlexDiv>
</FlexDiv>
