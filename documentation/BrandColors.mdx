import {Meta, Unstyled} from '@storybook/addon-docs/blocks';
import {FlexDiv} from "../src/components/FlexDiv";
import {BrandColors} from "./_components/BrandColors/BrandColors";

<Meta title="Colors/Core Colors" />

<FlexDiv width={"fill"} height={"fill"} alignItems={"center"}>
    <FlexDiv width={"fill"} maxWidth={760} height={"auto"}>
        <Unstyled>
            # Core Colors
            In a design language, the company brand typically provides a foundational pallet from which color schemes are derived. Slice's current light and dark themes, as well as any future themes (like high contrast) are derived from these core colors.
             
            Note: Slice's core colors are extended with "special" colors specific to digital product design and needs.
            
            <BrandColors />

            Note: While tokens are available via CSS in Slice for every core color, you should avoid referencing these directly and opt for referencing them bia theme based tokens.
        </Unstyled>   
    </FlexDiv>
</FlexDiv>
